description = 'basic-service'

dependencies {
    compile(project(':basic:basic-repo'))

    compile(
            "com.baomidou:mybatis-plus-boot-starter:${mybatisPlusVersion}",
            "com.alibaba:fastjson:1.2.73"
    )

}
