eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: test
    stream:
      bindings:
        myMessage:
          group: client
          content-type: application/json
  application:
    name: eureka-client